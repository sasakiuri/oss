---
"@sasakiuri/saika-director": patch
---

Keep interruption records, selection and mutation errors scoped to the current workspace. Ignore stale loads and completed operations after changing scopes or leaving the panel, and reload the ledger before retrying a failed operation.

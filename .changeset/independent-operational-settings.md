---
'@sasakiuri/saika-director': patch
---

Add independently configurable authentication and publication requirements to operational profiles. Expose supported modes per setting, preserve binary policies when choosing advisory checks, and report each applied or failed setting separately. Shared policy changes require competitions to be idle; authentication changes require a signed-in administrator.

---
description: Saika Lane が MT201 から受理するデータ形式、座標・得点への変換と対応範囲を説明します。
---

<!-- SPDX-License-Identifier: MIT -->

# MT201 受信互換仕様（Saika Lane）

[文書一覧](../../../../INDEX.md)

Saika Lane が `MT201` デバイス設定で受理する入力と、着弾への変換を説明する。メーカーの公式通信仕様ではない。

## 1. 適用範囲

### 1.1 公開情報と Saika 内の位置づけ

| 項目              | 内容                                                            |
| ----------------- | --------------------------------------------------------------- |
| 製品識別          | 興東電子株式会社 `MT-201`                                       |
| メーカー公開情報  | [ビームライフル装置](https://kohto.co.jp/beam03.html)           |
| Saika デバイス ID | `MT201`                                                         |
| Saika 対応種目    | `BEAM_RIFLE_10M`                                                |
| この文書の対象    | Saika Lane の受信フレーミング、構文検査、座標変換、ショット生成 |

メーカー公開ページでは、MT-201が得点と着弾位置を検出する標的装置として案内されている。本書にある
データ表現や変換係数は、メーカー仕様として引用するものではなく、Saika 公開実装の現在の挙動です。

標的面と採点データについては [標的・採点データ](../../../../common/TARGET_SPEC.md) も参照する。
BPT-216との実装差分は
[MT-201とのシグナル受信・処理比較](../bpt216/README.md#6-mt-201とのシグナル受信処理比較bp-217-if) にまとめる。

### 1.2 対象外

次の事項は、公開された Saika 実装と一次情報だけでは確定できないため仕様化しない。

- ホストから送るコマンドの機器側での意味、応答コード、動作モードの切り替え条件
- チェックサムの算出・検証アルゴリズム
- 校正、保守、ファームウェア更新、再起動の手順
- コネクター、ピン配列、電気特性、ケーブル長、電源、安全要件
- デバイス側の送信タイミング、再送、タイムアウト保証
- `R` / `S` が機器上で持つ正式名称や設定条件

これらを推測して実機へコマンドを送信しない。設置・保守・安全に関する判断にはメーカー提供資料を
使用する。

## 2. Saika 側のシリアル設定

Saika Lane のデバイス定義は次を既定値としている。これは Saika の設定値であり、メーカーによる
全構成への保証ではない。

| パラメーター   | Saika 既定値 |
| -------------- | -----------: |
| ボーレート     |         9600 |
| データビット   |            8 |
| ストップビット |            1 |
| パリティ       |       `none` |

正となる実装は
[`targetDeviceDefinitions.ts`](../../../../../saika-lane/src/main/modules/target/domain/targetDeviceDefinitions.ts)
です。Saika は OS が列挙したシリアルポートを利用者に選択させる。特定の USB 変換器、ポート名、
ドライバーはこの仕様の一部ではない。

Saika は接続時と試射・本射の切り替え時に、終端文字なしの ASCII `S` / `R` を送る。これは Saika の送信動作であり、機器側の正式なコマンド仕様を示すものではない。

## 3. 受信レコード

### 3.1 フレーミング

[`KohtoFormatParser`](../../../../../saika-lane/src/main/modules/target/infra/parsers/KohtoFormatParser.ts)
は受信バイト列を UTF-8文字列として扱い、LF（`\n`）ごとにレコードを分割する。

- LF まで到着した空でない行を1レコードとして後段へ渡す。
- 空行は無視する。
- 末尾に LF がない断片は `remaining` として保持し、次の受信データとの結合対象にする。
- 行末の CR や行の前後空白は、後段パーサーの `trim()` により除去されるため CRLF も受理できる。

文字列をバイト数18の固定長としては扱わない。フィールドの意味と受理条件を正とする。

### 3.2 正規形

Saika のテスト fixture で使う正規形は次です。

```text
R 9.7 0250 FF5F 70
R10.9 0000 0000 70
```

概念上の構文:

```text
record   = mode, optional-space, score, space, x-hex, space, y-hex, space, check-hex
mode     = "R" | "S"
score    = decimal-number
x-hex    = 4HEXDIG
y-hex    = 4HEXDIG
check-hex = 2HEXDIG
```

- モードと得点の間の空白は省略できる。
- 後続フィールドの間には1文字以上の空白が必要である。
- 座標とチェック欄の16進文字は小文字も受理し、内部では大文字へ正規化する。
- モードは現在の実装どおり大文字 `R` または `S` を使う。
- 送信側は得点を `0.0`〜`10.9` の小数点1桁で生成すること。最終的な範囲検査は Saika の
  `Score` 値オブジェクトが行う。

現在のパーサーは一部の空白や数値表現に寛容だが、それは互換性保証ではない。送信側は上記の正規形を
使い、不正な数値文字列が偶然受理される挙動に依存しない。

### 3.3 フィールド

| フィールド | 受理形式       | Saika での扱い                              |
| ---------- | -------------- | ------------------------------------------- |
| mode       | `R` または `S` | `R` → `MATCH`、`S` → `SIGHTING`             |
| score      | 数値文字列     | `round(score × 10)` で0.1点単位の整数へ変換 |
| x          | 16進4桁        | 符号付き16ビットへ変換後、mm へ変換         |
| y          | 16進4桁        | 符号付き16ビットへ変換後、mm へ変換         |
| check      | 16進2桁        | 形式だけを検査し、`Shot` には保存しない     |

チェック欄は2桁の16進数であることだけを確認する。Saika Lane は現在、値を再計算・照合しない。したがって、この欄が正しく見えても転送データの完全性は保証されない。

## 4. 座標変換

[`MT201CoordinateConverter`](../../../../../saika-lane/src/main/modules/target/adapters/mt201/MT201CoordinateConverter.ts)
は、X と Y を16ビットの2の補数として読み、次の式で Saika の mm 座標へ変換する。

```text
signed = hex < 0x8000 ? hex : hex - 0x10000
millimetres = signed / 150
```

| 16進値 | 符号付き値 | Saika 座標       |
| ------ | ---------: | ---------------- |
| `0000` |          0 | `0 mm`           |
| `0250` |        592 | 約 `3.947 mm`    |
| `FF5F` |       -161 | 約 `-1.073 mm`   |
| `8000` |     -32768 | 約 `-218.453 mm` |
| `7FFF` |      32767 | 約 `218.447 mm`  |

X/Y の符号を現実の左右・上下へ対応させる向きは、設置方向を含む実機側の条件を本書だけでは確定
できない。Saika は受信した X/Y の符号を反転せず、そのまま `ImpactPoint(x, y)` に格納する。

## 5. `Shot` への変換

[`MT201Adapter`](../../../../../saika-lane/src/main/modules/target/adapters/MT201Adapter.ts) は次の順で
レコードを変換する。

1. レコードを `mode`, `score`, `xHex`, `yHex`, `checksum` に分解する。
2. `R` を本射、`S` を試射として、呼び出し元のモードより受信モードを優先する。
3. ミスの特殊値を判定する。
4. 通常ショットの座標を mm へ変換する。
5. 得点を0.1点単位の整数に変換し、`0`〜`109` の範囲を検査する。
6. 種目の標的設計と座標から inner ten を判定し、`Shot` を生成する。

### 5.1 ミス

次の3条件がすべて成立するときだけ、Saika はミスとして扱う。

| score | x      | y      | 生成結果                                   |
| ----- | ------ | ------ | ------------------------------------------ |
| `0.0` | `7FFF` | `7FFF` | 得点0、`impactPoint: null`、inner ten なし |

座標の一方だけが `7FFF`、または得点が0以外の場合は、この特殊判定には入らない。

### 5.2 得点

受信得点を `Math.round(score × 10)` で整数化する。記録時には装置得点を優先し、座標からの計算値も照合用に保持する。インナー 10 は座標から判定する。共通の採点処理は [標的・採点データ](../../../../common/TARGET_SPEC.md#得点の扱い) を参照する。

## 6. エラー条件

次の入力は変換エラーまたは検証エラーになる。

- 必須フィールドが不足している、または余分な非空白文字がある。
- モードが大文字 `R` / `S` 以外である。
- 得点を有限数として解釈できない。
- X/Y が16進4桁ではない。
- チェック欄が16進2桁ではない。
- 変換後の得点が Saika の範囲 `0`〜`109` を外れる。
- 座標から有効な `ImpactPoint` を生成できない。

受信データをログへ出す場合は、機器固有情報や大会運用情報が混在していないことを確認し、公開の
issue や fixture へ実データをそのまま貼らない。

## 7. 実装確認用 fixture

次は Saika Lane の公開テストに収録済みの最小入力例です。実機ログを追加する場合の扱いは [公開コンテンツ方針](../../../../CONTENT_POLICY.md) を参照してください。

| 入力                 | 期待結果                                |
| -------------------- | --------------------------------------- |
| `R 9.7 0250 FF5F 70` | MATCH、9.7点、X≈3.947mm、Y≈-1.073mm     |
| `S10.5 00D0 FFC8 71` | SIGHTING、10.5点、X≈1.387mm、Y≈-0.373mm |
| `R10.9 0000 0000 70` | MATCH、10.9点、座標原点                 |
| `R 0.0 7FFF 7FFF 73` | ミス、座標なし                          |

確認対象:

- [MT-201直結プロトコル](../../../../../saika-lane/src/main/modules/connection/infra/usb/protocol/DirectSerialTargetProtocol.ts)
- [レコード構文テスト](../../../../../saika-lane/tests/unit/modules/target/adapters/mt201/MT201DataParser.test.ts)
- [座標変換テスト](../../../../../saika-lane/tests/unit/modules/target/adapters/mt201/MT201CoordinateConverter.test.ts)
- [アダプターテスト](../../../../../saika-lane/tests/unit/modules/target/adapters/MT201Adapter.test.ts)
- [改行フレーミングテスト](../../../../../saika-lane/tests/unit/modules/target/infra/parsers/KohtoFormatParser.test.ts)

## 8. 接続時の切り分け

1. Saika 設定で `MT201` と意図したシリアルポートが選択されているか確認する。
2. 設定値が9600 baud、8 data bits、parity none、1 stop bit になっているか確認する。
3. 受信行が LF または CRLF で完結しているか確認する。
4. モード、得点、X、Y、チェック欄が §3 の形式か確認する。
5. 形式が正しいのに拒否される場合は、匿名化した最小 fixture で公開テストを追加する。

OS 固有のドライバー導入、権限変更、配線、標的本体の保守は環境と製品資料に依存するため、本書では
一律の管理者コマンドや作業手順を提示しない。

## 9. 検証と変更管理

本書の受理条件は上記の実装と自動テストを根拠とする。自動テストの成功は、すべての OS・変換器・機器での動作保証を意味しない。利用する構成では接続、試射・本射の切り替え、連続着弾、再接続を確認する。

受理形式を変更した場合は、同じ変更で本書と対応するテストを更新する。公開する入力例や資料の扱いは [公開コンテンツ方針](../../../../CONTENT_POLICY.md) に従う。製品名・会社名の権利は各権利者に帰属する。
